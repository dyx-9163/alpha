# TLS files

`fullchain.pem` and `privkey.pem` are the bundled self-signed bootstrap certificate for `aifar.local`.

Replace both files with a trusted certificate and matching private key before production use, then run `../reload.sh`.
