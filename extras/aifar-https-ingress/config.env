AIFAR_HTTPS_CONTAINER_NAME=aifar-https-ingress
AIFAR_HTTPS_IMAGE=nginx:stable-alpine
AIFAR_HTTPS_WEB_PORT=8080
AIFAR_HTTPS_GATEWAY_PORT=38000
AIFAR_HTTPS_CONFIGURE_FIREWALL=1
# Leave empty to use the default-route interface zone, then the default zone.
AIFAR_HTTPS_FIREWALL_ZONE=
