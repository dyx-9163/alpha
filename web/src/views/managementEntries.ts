export const visibleManagementTabs = {
  database: ['instances'],
  nacos: ['instances'],
  storage: ['instances']
} as const

export const visibleManagementHeaderActions = {
  database: ['connected'],
  nacos: ['connected'],
  storage: ['connected']
} as const
