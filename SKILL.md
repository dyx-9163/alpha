---
name: aifar-module-business-definition
description: Use when defining, refining, or reviewing actual business behavior for AIFAR Deployment functional modules. 用于定义服务器面板/运维工具模块的真实业务，包括网站、数据库、容器、应用商店、文件管理、计划任务、备份、安全、监控、终端、插件和 AI 自动化。
---

# AIFAR Module Business Definition

当用户要定义某个功能模块“实际要做什么”时，先使用本 skill。输出结果应该成为 API、UI 流程、权限、任务、测试和验收标准的业务源头。

## 使用流程

1. 识别模块名称、目标用户、被管理资源和业务目标。
2. 阅读 `AGENTS.md`，让模块定义对齐项目架构、核心模块清单、安全规则和 API/事件约定。
3. 使用下方模板产出一张业务模块卡。
4. 将模糊能力拆成具体用户操作。
5. 给每个操作标记类型：`read-only`、`state-changing`、`long-running`、`dangerous`、`scheduled`、`plugin-provided`、`AI-assisted`。
6. 定义支撑这些操作所需的最小数据模型。
7. 定义 API 边界、领域事件、审计事件、权限键和任务行为。
8. 在建议实现前，补齐验收标准和测试场景。

## 模块卡模板

```markdown
# 模块：<模块名称>

## 业务目标
- <这个模块替用户解决什么运维问题>

## 角色与权限
- Owner:
- Admin:
- Operator:
- Viewer:

## 资源对象
- 主资源：
- 关联资源：
- 外部依赖：

## 核心操作
| 操作 | 类型 | 入口 | 前置条件 | 成功结果 | 失败处理 |
| --- | --- | --- | --- | --- | --- |
| <创建/启动/停止/备份/恢复/删除等> | <read-only/state-changing/long-running/dangerous/scheduled/plugin-provided/AI-assisted> | <页面/API/任务/插件> | <条件> | <结果> | <错误与回滚> |

## 数据模型草案
| 实体 | 字段 | 说明 |
| --- | --- | --- |
| <Entity> | <field:type> | <business meaning> |

## API 草案
- `GET /api/v1/<module>`：<说明>
- `POST /api/v1/<module>`：<说明>
- `POST /api/v1/<module>/{id}/<action>`：<说明>

## 任务与事件
- 长任务：
- 领域事件：
- 审计事件：
- 通知事件：

## 安全与风控
- 危险操作：
- 二次确认：
- 参数白名单：
- 凭据处理：
- 日志脱敏：

## UI 工作流
1. <用户进入哪里>
2. <用户选择或输入什么>
3. <系统展示什么反馈>
4. <完成后用户如何验证结果>

## 验收标准
- Given <前置条件>, When <用户动作>, Then <期望结果>

## 非目标
- <本阶段明确不做什么>
```

## 业务规则

- 把每个服务器操作都当成业务流程，而不是简单 shell 命令。
- 先定义资源、权限、状态流转和失败行为，再定义 UI。
- 破坏性操作必须定义确认文案、可恢复性、备份预期和审计事件。
- 长任务必须定义状态值、进度消息、重试规则、取消行为和日志可见性。
- 凭据必须定义存储、脱敏、轮换和删除行为。
- 插件必须定义能力申请、沙箱边界、安装/更新/卸载生命周期和回滚行为。
- AI 辅助操作默认需要预览步骤；只有只读操作可以直接给出结果。

## 标准操作类型

- `read-only`：查询状态，不改变主机、数据库、文件系统、容器或配置。
- `state-changing`：改变本地元数据或被管理系统状态。
- `long-running`：需要任务 ID、进度、日志、超时、取消和重试策略。
- `dangerous`：可能导致数据丢失、服务暴露、安全弱化或运行中负载中断。
- `scheduled`：由 cron/timer 触发，必须定义错过执行窗口后的行为。
- `plugin-provided`：来自插件清单，必须声明权限。
- `AI-assisted`：使用 AI 做诊断、生成或推荐，必须展示输入来源和输出预览。

## 输出质量标准

- 好的模块定义应该让另一个 agent 可以直接实现，不需要猜业务意图。
- 好的模块定义必须说明成功、部分成功、失败、重试、取消和权限拒绝时分别发生什么。
- 好的模块定义需要为每个核心操作提供至少一条验收标准。
- 好的模块定义不盲目复制 1Panel 或宝塔行为，而是把它们作为参考点，适配本项目架构。
