# AIFAR Memory Archive — 2026-08-26

本文件由 memory.md 写入守卫自动生成，记录该日全部问题与结论。禁止手动编辑；如需修正，改 memory.md 当天段后让守卫重新归档。

## 2026-08-26
- 问题：需要扫描全仓代码，并从企业级运维方向为当前 AIFAR Deployment 生成标准 SDD 改造方案。
- 结论：采用当前仓库渐进式双 Profile 路线，保留 Go/Vue、离线交付和 Standalone 单二进制，同时分阶段建设 SSH 主机身份、持久任务租约与 fencing、标准变更审批、统一资源模型、可观测性、全栈灾备和 PostgreSQL Enterprise Profile；完整设计见 `docs/superpowers/specs/2026-08-26-aifar-enterprise-operations-transformation-design.md`。
- 问题：AI 客户端应直接调用抽离的 Shell 脚本，还是通过 API 执行运维能力。
- 结论：采用 API First、MCP 工具门面、受控执行器分层；AI 只调用有类型、有权限、有风险等级和审计语义的工具/API，Shell 仅作为服务端执行器的签名版本化实现，禁止向 AI 暴露通用自由 Shell。
- 问题：担心 API 模式需要新增独立服务，而客户端内置 Shell 更便于持续优化。
- 结论：API 是能力边界而非独立部署要求；在现有 aifar-server 内增加通用 Operation API/MCP 门面，并把 Shell、HTTP、Provider 和工作流实现封装为可签名、版本化、热更新的 Operation Pack，可兼顾快速迭代与集中鉴权、审批、任务和审计。
- 问题：担心当前 AIFAR 服务健壮性不足，无法安全进入生产环境。
- 结论：当前代码只能定位为开发、测试或受控试点，尚不能给出企业生产就绪结论；先暂停 AI 写操作和非必要能力扩张，以 Phase 0 的 SSH 指纹校验、持久任务租约与 fencing、不可抵赖审计、TLS/token 安全、备份恢复和故障注入作为生产准入阻断门禁，再按 Standalone 与 Enterprise Profile 分级验收。
